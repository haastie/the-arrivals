# Supabase — kopieer naar .env.local en vul in (Project Settings → API).
# In Vercel: zet deze als Environment Variables (Production + Preview).
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
